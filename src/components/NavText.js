import React from 'react'

export default function NavText({ children }) {
  return (
    <span
      className="font-normal font-heading text-xl xl:text-2xl text-[#322203] uppercase"
    >
      {children}
    </span> 
  )
}
